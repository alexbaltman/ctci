'''
Given a boolean expression consisting of the symbols 0 (false), 1 (true), & (AND), | (OR), and ^ (XOR), and a desired boolean result value result, implement a function to cont the number of ways of parenthesizing the expression such that it evaluates to result.

Example:
    countEval("1A^0|0|1", false) -> 2
    countEval("0&0&0&1^1|0", true) -> 10

Hints: 148, 168, 197, 305, 327
'''


def boolean_eval(s):
    pass

if __name__ == '__main__':
    assert boolean_eval('') ==
    assert boolean_eval('') ==
    assert boolean_eval('') ==
    assert boolean_eval('') ==
    assert boolean_eval('') ==
