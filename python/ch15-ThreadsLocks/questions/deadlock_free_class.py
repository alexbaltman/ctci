'''
Design a class which provides a lock only if there are no possible deadlocks.

Hints: 422, 434
'''


def deadlock_free_class(s):
    pass

if __name__ == '__main__':
    assert deadlock_free_class('') ==
    assert deadlock_free_class('') ==
    assert deadlock_free_class('') ==
    assert deadlock_free_class('') ==
    assert deadlock_free_class('') ==
