'''
A magic index in an array A[0...n-1] is defined to be an index such that A[1] = i. Given a sorted array of distinct integers, write a method to find a magic index, if one exists, in array A.

Follow Up: What if the values are not distinct?

Hints: 170, 204, 240, 286, 340
'''


def magicindex(s):
    pass

if __name__ == '__main__':
    assert magicindex('') ==
    assert magicindex('') ==
    assert magicindex('') ==
    assert magicindex('') ==
    assert magicindex('') ==
