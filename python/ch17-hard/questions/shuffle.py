'''
Write a method to shuffle a deck of cards. It must be a perfect shuffle - in other words, each of the 52! permutations of the deck has to be equally likely. Assume that you are given a random number generators which is perfect.

Hints: 483, 579, 634
'''


def shuffle(s):
    pass

if __name__ == '__main__':
    assert shuffle('') ==
    assert shuffle('') ==
    assert shuffle('') ==
    assert shuffle('') ==
    assert shuffle('') ==
