'''
Write a function that adds two numbers. You should not use + or any arithmetic operators.

Hints: 467, 544, 601, 628, 642, 664, 692, 712, 724
'''


def add_without_plus(s):
    pass

if __name__ == '__main__':
    assert add_without_plus('') ==
    assert add_without_plus('') ==
    assert add_without_plus('') ==
    assert add_without_plus('') ==
    assert add_without_plus('') ==
