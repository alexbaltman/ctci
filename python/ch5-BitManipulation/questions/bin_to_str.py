'''
Given a real number between 0 and 1 (e.g. 0.72) that is passed in as a double, print the binary representation. If the number cannot be represented accurately in a binary with at most 32 characters, print "ERROR".

Hints: 143, 167, 173, 269, 297
'''


def bin_to_str(s):
    pass

if __name__ == '__main__':
    assert bin_to_str('') ==
    assert bin_to_str('') ==
    assert bin_to_str('') ==
    assert bin_to_str('') ==
    assert bin_to_str('') ==
