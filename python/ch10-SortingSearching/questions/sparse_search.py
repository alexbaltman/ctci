'''
Given a sorted array of strings that is interspersed with empty strings, write a method to find the location of a given string.

Example:
    Input: ball, {"at", "", "", "", "ball", "", "", "car", "", "", "dad", "", ""}
    Output: 4

Hints: 256
'''


def sparse_search(s):
    pass

if __name__ == '__main__':
    assert sparse_search('') ==
    assert sparse_search('') ==
    assert sparse_search('') ==
    assert sparse_search('') ==
    assert sparse_search('') ==
