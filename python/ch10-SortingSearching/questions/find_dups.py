'''
You have an array with all the numbers from 1 to N, whee N is at most 32,000. The array may have duplicate entries and you do not know what N is. With only 4 kilobytes of memory available, how would you print all duplicate elements in the array?

Hints: 289, 315
'''


def find_dups(s):
    pass

if __name__ == '__main__':
    assert find_dups('') ==
    assert find_dups('') ==
    assert find_dups('') ==
    assert find_dups('') ==
    assert find_dups('') ==
