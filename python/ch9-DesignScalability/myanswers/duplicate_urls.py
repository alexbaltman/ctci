'''
You have 10 billion URLs. How do you detect the duplicate documents? in this case, assume "duplicate" means that the URLs are identical.

Hints: 326, 347
'''


def duplicate_urls(s):
    pass

if __name__ == '__main__':
    assert duplicate_urls('') ==
    assert duplicate_urls('') ==
    assert duplicate_urls('') ==
    assert duplicate_urls('') ==
    assert duplicate_urls('') ==
