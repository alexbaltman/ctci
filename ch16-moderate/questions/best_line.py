'''
Give a two-dimensional graph with points on it, find a line which passes the most number of points.

Hints: 491, 520, 529, 563
'''


def best_line(s):
    pass

if __name__ == '__main__':
    assert best_line('') ==
    assert best_line('') ==
    assert best_line('') ==
    assert best_line('') ==
    assert best_line('') ==
